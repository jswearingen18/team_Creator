const Manager = require('../manager');

describe("Manager", () => {
    describe("Should run if user inputs Manager for role", () => {
      it("retrieve user input for data", () => {
     
        expect(name).toEqual(name);
        expect(id).toEqual(id);
        expect(email).toEqual(email);
        expect(officeNumber).toEqual(officeNumber);
      });
    });